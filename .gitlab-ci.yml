image: node:12

stages:
  - install
  - build
  - test
  - deploy

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - lib/node_modules/
    - demo-app/node_modules/

install:
  stage: install
  script:
    - cd lib
    - npm install
    - cd ../demo-app
    - npm install

test lib:
  stage: test
  script:
    - cd lib
    - npm run test
  artifacts:
    paths:
      - lib/test-report.xml
    reports:
      junit: lib/test-report.xml

build lib:
  stage: build
  script:
    - cd lib
    - npm run build

build demo:
  stage: build
  script:
    - cd demo-app
    - npm run build
  artifacts:
    paths:
      - dist

pages:
  stage: deploy
  dependencies:
    - build demo
  script:
    - cp -r dist ../public
  artifacts:
    paths:
      - public
  only:
    - master
